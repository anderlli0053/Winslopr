<?xml version="1.0" encoding="utf-8"?>
<ContentDialog  CornerRadius="12"
    x:Class="Winslopr.PluginsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Winslopr"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Uid="PluginsDialog"
    DefaultButton="Close"
    MinWidth="470">

    <Grid Width="450" RowDefinitions="Auto,*,76,Auto,Auto,Auto" MinHeight="390">
        <Grid.Resources>
            <Style TargetType="Button">
                <Setter Property="Padding" Value="10,5" />
                <Setter Property="CornerRadius" Value="10" />
                <Setter Property="FontSize" Value="11.5" />
            </Style>
            <Style TargetType="TextBlock">
                <Setter Property="FontSize" Value="11.5" />
            </Style>
        </Grid.Resources>

        <Grid.Background>
            <AcrylicBrush TintColor="{ThemeResource SolidBackgroundFillColorBase}"
                          TintOpacity="0.58"
                          TintLuminosityOpacity="0.88"
                          FallbackColor="{ThemeResource SolidBackgroundFillColorBase}" />
        </Grid.Background>

        <!-- Search -->
        <TextBox x:Name="txtSearch" Grid.Row="0" x:Uid="TxtSearch"
                 Margin="0,0,0,8"
                 Padding="10,6"
                 FontSize="12"
                 CornerRadius="12"
                 TextChanged="txtSearch_TextChanged" />

        <!-- Plugin list with headers -->
        <Border Grid.Row="1"
                Margin="0,0,0,8"
                CornerRadius="16"
                BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}"
                BorderThickness="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <Grid RowDefinitions="Auto,*">
                <Grid Grid.Row="0" ColumnDefinitions="28,*,68,42" Padding="8,5,8,6"
                  BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" BorderThickness="0,0,0,1">
                <TextBlock Grid.Column="1" x:Uid="ColPlugin" FontSize="11"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock Grid.Column="2" x:Uid="ColStatus" FontSize="11"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                <TextBlock Grid.Column="3" x:Uid="ColType" FontSize="11"
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </Grid>
            <ListView Grid.Row="1" x:Name="listPlugins" Height="180"
                      Padding="0,2,0,0"
                      SelectionMode="Single" SelectionChanged="listPlugins_SelectionChanged">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:PluginEntry">
                        <Grid ColumnDefinitions="28,*,68,42" Padding="4,3">
                            <CheckBox IsChecked="{Binding IsChecked, Mode=TwoWay}" MinWidth="0" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{Binding Name}" VerticalAlignment="Center" Margin="4,0"
                                       FontSize="11.5" TextTrimming="CharacterEllipsis" />
                            <TextBlock Grid.Column="2" Text="{Binding InstalledText}" VerticalAlignment="Center"
                                       Margin="4,0" Foreground="{Binding InstalledColor}" FontSize="10.5" />
                            <TextBlock Grid.Column="3" Text="{Binding Type}" VerticalAlignment="Center"
                                       Margin="4,0" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="10.5" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            </Grid>
        </Border>

        <!-- Description -->
        <ScrollViewer Grid.Row="2" Margin="0,0,0,8"
                      BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}"
                      BorderThickness="1"
                      CornerRadius="14"
                      Padding="8">
            <TextBlock x:Name="txtDescription" TextWrapping="Wrap" FontSize="11" Opacity="0.82" />
        </ScrollViewer>

        <!-- Progress -->
        <ProgressBar x:Name="progressBar" Grid.Row="3" Visibility="Collapsed" Margin="0,0,0,8" />

        <!-- Buttons row 1 -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="6" Margin="0,0,0,6">
            <Button x:Uid="BtnPluginInstall"    Click="btnInstall_Click" />
            <Button x:Uid="BtnPluginUpdateAll"  Click="btnUpdateAll_Click" />
            <Button x:Uid="BtnPluginRemove"     Click="btnRemove_Click" />
        </StackPanel>

        <!-- Buttons row 2 -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Spacing="6">
            <Button x:Uid="BtnPluginEdit"       Click="btnEdit_Click" />
            <Button x:Uid="BtnPluginOpenFolder" Click="btnOpenFolder_Click" />
            <Button x:Uid="BtnPluginSubmit"     Click="btnSubmit_Click" />
        </StackPanel>

    </Grid>
</ContentDialog>
